INJECT_OR_CREATE:building_intel_hq = {
	building_group = bg_intel_buildings
	city_type = city
	ownership_type = self
	icon = "gfx/interface/icons/building_icons/secret_service_hq.dds"
	background = "gfx/interface/icons/building_icons/backgrounds/building_panel_bg_monuments.dds"

	# has_max_level = yes needs variable to be implemented with relative modifier
	# levels_per_mesh = 50
	
	unlocking_technologies = { HMS_tech_era_2_secret_service }
	required_construction = construction_cost_high
	

	production_method_groups = {
		hms_pmg_group_1
		# pmg_group_3 moved to university
	}

	# Not possible if owner has No Home Affairs

	possible = {
		owner = {
			NOT = {
				OR = {
					has_law = law_type:law_no_home_affairs
				}
			}
		}
	}
	
	# What the building provides to the country
	# country_modifiers = {
	# 	workforce_scaled = {
	# 		country_bureaucracy_add = 25
	# 	}
		
	# 	level_scaled = {
	# 		country_prestige_add = 10
	# 		country_influence_add = 5
	# 	}
	# }

	ai_value = {
		value = 2

		# Great powers prioritize it
		if = {
			limit = { is_great_power = yes }
			multiply = 1.3
		}
		
		# Higher priority if in rivalries
		if = {
			limit = {
				any_scope_country = {
					has_diplomatic_pact = {
						who = ROOT
						type = rivalry
					}
				}
			}
			multiply = 1.4
		}
	}
	
}

INJECT_OR_CREATE:building_military_intel_hq = {
	building_group = bg_intel_buildings
	city_type = city
	ownership_type = self

	icon = "gfx/interface/icons/building_icons/military_intel_hq.dds"
	background = "gfx/interface/icons/building_icons/backgrounds/building_panel_bg_monuments.dds"

	# has_max_level = yes needs variable to be implemented with relative modifier
	# levels_per_mesh = 50
	
	unlocking_technologies = { military_statistics }
	required_construction = construction_cost_high
	

	production_method_groups = {
		hms_pmg_group_2
	}

	# country_modifiers = {
	# 	workforce_scaled = {
	# 		country_bureaucracy_add = 25
	# 	}
		
	# 	level_scaled = {
	# 		country_prestige_add = 10
	# 		country_influence_add = 5
	# 	}
	# }

	ai_value = {
		value = 2

		# Great powers prioritize it
		if = {
			limit = { is_great_power = yes }
			multiply = 1.3
		}
		
		# Higher priority if in rivalries
		if = {
			limit = {
				any_scope_country = {
					has_diplomatic_pact = {
						who = ROOT
						type = rivalry
					}
				}
			}
			multiply = 1.4
		}
	}
}

REPLACE_OR_CREATE:building_university = {

	building_group = bg_technology

	city_type = city

	levels_per_mesh = 50

	unlocking_technologies = {
		academia
	}

	production_method_groups = {
		pmg_base_building_university
		pmg_university_academia
		pmg_principle_freedom_of_movement_3
		hms_pmg_group_3
	}

	icon = "gfx/interface/icons/building_icons/building_university.dds"

	required_construction = construction_cost_medium

	background = "gfx/interface/icons/building_icons/backgrounds/building_panel_bg_monuments.dds"

	ai_value = {
		value = 1000

		if = { # British dictates # dev stuff do not touch
			limit = {
				owner = {
					has_journal_entry = je_british_dictate_universities
				}
				OR = {
					state_region = s:STATE_WEST_BENGAL
					state_region = s:STATE_BOMBAY
					state_region = s:STATE_MADRAS
				}
				NOR = {
					has_building = building_university
					any_scope_building = {
						is_building_type = building_university
						level > 3
					}
				}
			}
			add = 2000
		}
	}
}
