### Joint Scientific Publication — Two Progress Bars ###
### Driven entirely by scripted effects calling add_progress ###

breakthrough_progress_bar = {
    name = "breakthrough_progress_bar"
    desc = "breakthrough_progress_bar_desc"

    default = yes

    start_value = 0
    min_value = 0
    max_value = 100
}

setbacks_progress_bar = {
    name = "setbacks_progress_bar"
    desc = "setbacks_progress_bar_desc"

    default_bad = yes

    start_value = 0
    min_value = 0
    max_value = 100
}